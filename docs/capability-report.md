# Capability report

_Not generated yet._

Run discovery to populate this file with the exact MCP tools your robot exposes and the
relevant Home Assistant entities:

```bash
cd core && python scripts/discover.py
```

This is the ground-truth contract the real robot driver is built against.
