name: pomodoro
description: Work/break timer. Announces each phase change (face + LED colour + a spoken line).
kind: foreground
entrypoint: mode:PomodoroMode
enabled: true
config:
  work_minutes: 25
  break_minutes: 5
  work_color: red
  break_color: green
